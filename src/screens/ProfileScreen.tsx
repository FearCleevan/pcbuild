import ScreenContainer from '../components/layout/ScreenContainer';

export default function ProfileScreen() {
    return (
        <ScreenContainer
            title='Profile'
            subtitle='Sample profile screen route. Add user details, saved builds, and settings in this section.'
        />
    );
}
